# SPDX-FileCopyrightText: 2025 GitHub
# SPDX-License-Identifier: MIT

seclab-taskflow-agent:
  version: "1.0"
  filetype: taskflow

taskflow:
  - task:
      agents:
        - tests.data.test_yaml_parser_personality000
      user_prompt: |
        This is a reusable taskflow.
        Global: {{ globals.reusable_global }}
        Input: {{ inputs.reusable_input }}
